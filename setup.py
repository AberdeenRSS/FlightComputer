#!/usr/bin/env python

from setuptools import find_packages, setup

# packages = [f'flight_computer_core.core.{pck}' for pck in  find_packages(where='core')]
# dirs = dict([(f'flight_computer_core.core.{pck}', 'core/' + pck.replace('.','/')) for pck in  find_packages(where='core')])


setup(name='flight_computer',
      version='0.1',
      description='',
      author='',
      author_email='',
      url='',
      packages=['flight_computer'],
     )